name: Windows Build
on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: '6.5.0'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_msvc2019_64'

    - name: Build
      run: |
        cd ClientGui
        mkdir build
        cd build
        cmake ..
        cmake --build . --config Release

    - name: Deploy
      run: |
        cd ClientGui/build
        windeployqt --release --no-translations --no-opengl-sw --no-compiler-runtime Release\gui_client.exe --dir deploy
        Copy-Item Release\gui_client.exe -Destination deploy\

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Windows-Game-Release
        path: ClientGui/build/deploy
